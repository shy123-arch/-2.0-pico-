[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tianyi2-pico-teleop"
version = "0.1.0"
description = "Fail-closed PICO teleoperation bridge for X-Humanoid Tianyi 2.0"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]

[project.optional-dependencies]
ik = ["pin>=2.6"]
